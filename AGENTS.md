# 黑白棋项目约定

- 本目录为独立项目，操作在此执行，不修改其他游戏实现。
- 主标题只用“黑白棋”；芭乐与青提仅为棋子阵营。首页不得出现棋盘。
- 当前设计为 Pixso `V2` 画板：创建／加入、候场、大棋盘在左的信息右栏布局，手机棋盘居中优先。先改 Pixso，再改代码，最后在授权范围内部署。
- Pixso 每次写入必须在同一脚本确认文件名为“芭乐与青提 · 黑白棋”，防止其他任务切换活动文件造成跨项目操作；该名称仅为既有设计文件标识，非产品主标题。
- 候场和每局结束都提供交换先后手；两人在场需对方确认，进行中禁止交换，历史比分和胜负绑定本局阵营，不能跟随下一局席位变动。
- 对局中提供“认输”，确认后双方结算；邀请链接只在候场页。重开在线对局必须双方同意。
- 使用原生 HTML/CSS/JS 与 Node.js 内置模块，无第三方运行依赖。在线与本地共用 `engine.js`，规则见 `rule.md`。
- 在线以服务端为准；保护令牌、版本、请求幂等、持久化和断线恢复。轮询相同版本不重复渲染或播放动画。
- 保持同屏、人机模式可用。修改后检查键盘、鼠标与触控路径；浏览器尺寸模拟不等于真机测试。
- 原始棋子图没有 alpha，使用真实纹章素材的圆形裁切，不生成方形底图或通用符号替代。
- 验证 `npm test`、`npm run check`、`npm run build`；UI 变更检查桌面、平板、手机。
- `.local/`、`.data/`、`dist/` 不提交。源文件与可分享文档不得包含私人域名、邮箱、用户名、绝对路径、设计链接、服务器地址或秘密。
- 发布仅使用白名单产物与哈希校验；备份并支持回滚，不覆盖房间存档。只在明确授权时部署、提交或推送。
- 文本末尾不增加空行。
