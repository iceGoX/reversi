# 黑白棋 V2 简版验收

final result: passed

## 本轮完成

先更新 Pixso V2 画板，再修改代码并发布。产品名改为“黑白棋”；首页没有棋盘，包含创建／加入房间与昵称、房间号输入。对局棋盘置左并放大，信息和操作集中在右侧；手机棋盘居中优先。

- 在线双人：创建、加入、房主开局、落子同步、认输、再来一局。
- 认输需确认；邀请链接仅保留在候场页。
- 候场及每局结束均可交换先后手。两人在场需对方确认；可拒绝或取消。独自在候场可直接选择。
- 交换只影响下一局；本局棋盘、比分、玩家归属和胜负保持不变。进行中不允许交换，待处理的交换申请会阻止开始下一局。

## UI 检查

| 画面 | 实际检查与证据 |
| --- | --- |
| 桌面首页 | 1440 × 1024，无棋盘；两栏信息、输入框、实心主按钮与次要入口间距清晰。`design/evidence/v2/home-desktop.png` |
| 桌面对局 | 大棋盘约 800px，左侧棋盘、右侧回合和分数；认输与离开按钮分开。`game-desktop.png` |
| 手机首页 | 390 × 844，创建／加入表单无横向溢出，主操作完整。公网截图 `public-home-mobile.png` |
| 平板 | 834 × 1194，棋盘 480px、右侧信息栏 250px，无横向溢出或遮挡。`game-tablet.png` |
| 交换 | 双窗口确认后的先手权限正确；结算弹窗出现下一局先后手说明和交换按钮。`swap-confirm.png`、`result-swap.png` |

证据文件仅保留在本地 `design/evidence/v2/`，不随源码仓库上传。对应可编辑 Pixso 的 V2 首页、对局及交换画板预览也保存在该目录。对照了中文标题层级、奶油／绿配色、输入和按钮高度、棋盘比例及瓷质纹章，无未解决的 P0/P1/P2 UI 问题；非逐像素复刻，系统字体的笔锋可能略有差异。

## 简单逻辑与发布验证

- `npm test`：12 项通过，包括既有规则、网络双人、认输、协商交换、结算不变、交换后的实际先手，以及发布软链接启动回归。
- 双窗口浏览器：创建／加入、首步 4 : 1 同步、认输双方相同结果；候场交换后加入者成功获得先手。
- 公网冒烟：前端与素材 SHA-256、服务端发布号、创建／加入、落子同步、认输、结束后交换、再来一局及新先手落子均通过。
- 最终版本：`83deea2c2a5dac40`，与本地 `dist/release.json` 一致。Nginx 语法通过，独立服务已启动；已有主页与各游戏返回 HTTP 200。
- 首次发布曾因软链接入口判定未启动，自动回滚；修正后增加回归测试，后续两次发布均通过健康检查。存档目录独立，更新没有覆盖房间数据。

以上为当时的部署验收记录，不代表当前线上状态。浏览器尺寸检查不代表移动真机验收，本轮未做长时间或大规模并发测试。
